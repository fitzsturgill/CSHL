function ax = lickRaster(SessionData, outcome, type, fig)
% make SessionData global eventually???
    if nargin < 4
        fig = figure; % create figure
    else
        figure(fig); % or bring it to front
    end
    
    trials = filterTrials(SessionData, type, outcome);
    
    lickTimes = [];
    lickTrials = [];
    for i = 1:length(trials)
        trial = trials(i);
        if isfield(SessionData.RawEvents.Trial{trial}.Events, 'Port2In')
            lickTimes = 
        
        
        
%         for i = 1:nTrials
%     if isfield(SessionData.RawEvents.Trial{i}.Events, 'Port2In')
%         nLicks(i) = length(SessionData.RawEvents.Trial{i}.Events.Port2In);
%     else
%         nLicks(i) = 0;
%     end
% end
